import styled from 'styled-components';

const Sidebar = () => {
  return (
    <WrapSidebar>
      <div>Sidebar</div>
    </WrapSidebar>
  );
};

const WrapSidebar = styled.aside``;

export default Sidebar;
